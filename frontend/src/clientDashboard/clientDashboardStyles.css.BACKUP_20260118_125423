
/* ================================
   AUTH BLUE – SINGLE SOURCE OF TRUTH
   ================================ */
.client-root{
  --zy-accent: #00ffff;
  --zy-accent-soft: rgba(0,255,255,0.35);
  --zy-accent-border: rgba(0,255,255,0.55);
  --zy-accent-glow: 0 0 28px rgba(0,255,255,0.45);
}


html, body {
  height: auto !important;
  min-height: 100%;
  overflow-y: auto !important;
}
.client-root {
  --bg0:#070012;
  --bg1:#0b0020;
  --bg2:#140033;

  --glass: rgba(255,255,255,0.06);
  --glass2: rgba(0,0,0,0.35);
  --stroke: rgba(255,255,255,0.10);

  --text:#f6f2ff;
  --muted: rgba(255,255,255,0.72);

  --neonA:#00ffff;
  --neonB:#00ffff;
  --neonC:#00ffff;

  --shadow: 0 18px 60px rgba(0,0,0,0.55);
  --glow: 0 0 36px rgba(0,255,255,0.35);
  --glow2: 0 0 28px rgba(0,255,255,0.22);
}

*{ box-sizing:border-box; }


  min-height:auto;
  background:
    radial-gradient(900px 420px at 58% 6%, rgba(0,229,255,0.22), transparent 70%),
    radial-gradient(700px 360px at 18% 18%, rgba(0,255,255,0.12), transparent 65%),
    
    linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72));

  background: rgba(255,45,85,0.22);
}

html, body{
  height: auto;
  min-height: 100%;
  overflow-y: auto;
  overscroll-behavior: auto;

body{
  margin:0;
  color:var(--text);
  font-family:'Space Grotesk', system-ui, -apple-system, Segoe UI, Roboto, sans-serif;

  
}

.client-root .app{
  min-height:auto;
padding: 30px;
  max-width: 1320px;
  margin: 0 auto;
}

.client-root .header{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom: 26px;
}

.client-root .header h1{
  margin:0;
  font-family:'Orbitron', ui-sans-serif, system-ui;
  font-weight:700;
  letter-spacing: 0.10em;
  text-transform: uppercase;
  font-size: 34px;
  text-shadow: 0 0 18px rgba(0,229,255,0.28);
}

.client-root .status{
  font-family:'Orbitron', ui-sans-serif, system-ui;
  letter-spacing: 0.08em;
  font-size: 12px;
  padding: 8px 14px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,0.12);
    box-shadow: -8px 0 30px rgba(0,0,0,0.6);
}

.client-root .grid{
  grid-row-gap: 32px;
  display:grid;
  align-content: start;

  grid-template-columns: repeat(3, 1fr);
  column-gap: 22px;
  row-gap: 32px;
  /* IMPORTANT: no fixed row heights -> prevents chat overflow */
  grid-auto-rows: auto;
}

.client-root .panel{
  position: relative;
  border-radius: 22px;
  padding: 22px;

  background:
    linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72)) 12%, transparent),
      rgba(0,0,0,0.68)
    );

  border: 1px solid color-mix(in srgb, var(--zy-accent) 55%, transparent);
  backdrop-filter: blur(18px);

  box-shadow:
    0 0 36px color-mix(in srgb, var(--zy-accent) 55%, transparent),
    inset 0 0 18px color-mix(in srgb, var(--zy-accent) 25%, transparent);

  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.client-root .panel:has(.radar-lanes) {
  grid-row: span 2;
}


.client-root .panel::before{
  content:"";
  position:absolute;
  inset:-2px;
  pointer-events:none;
  background:
    radial-gradient(
      420px 220px at 50% 0%,
      rgba(0,255,255,0.12),
      transparent 65%
    );
  opacity:0.9;
}


.client-root .panel > *{ position:relative; z-index:1; }

.client-root .panel h2{
  margin:0 0 14px 0;
  font-family:'Orbitron', ui-sans-serif, system-ui;
  letter-spacing: 0.06em;
  font-weight:700;
  font-size: 16px;
  text-transform: uppercase;
  color: rgba(255,255,255,0.92);
}

.client-root .panel p{
  margin: 8px 0;
  color: var(--muted);
}

.client-root .controls{
  display:flex;
  flex-direction:column;
  gap: 14px;
}

.client-root .btn{
  
  border:none;
  border-radius: 16px;
  padding: 14px 16px;
  font-size: 14px;
  cursor:pointer;
  color: rgba(255,255,255,0.92);
  background: rgba(10,0,20,0.55);
  border: 1px solid rgba(255,255,255,0.10);
  box-shadow: inset 0 0 20px rgba(255,255,255,0.04);
  transition: transform 120ms ease, filter 120ms ease;
}

.client-root .btn:active{ transform: translateY(1px); }

.client-root .btn.primary{
  background: linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72)), var(--neonB));
  box-shadow: 0 0 30px rgba(0,255,255,0.55), 0 0 18px rgba(0,255,255,0.22);
}

.client-root .inner-glass{
  flex:1;
  border-radius:16px;

  background:
    linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72)),
      rgba(0,0,0,0.7)
    );

  border:1px solid rgba(0,255,255,0.35);
  box-shadow:
    inset 0 0 20px rgba(0,255,255,0.12);

  padding:14px;
  color: rgba(200,255,255,0.9);
}


.client-root .chat-box{
  height: calc(100% - 36px);
  display:flex;
  flex-direction:column;
  gap: 12px;
  min-height: 0; /* critical for flex overflow */
}
/* ===== RADAR HEIGHT ANCHOR ===== */



.client-root .chat-messages{
  flex: 1;
  min-height: 0;
  background: var(--glass2);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 16px;
  padding: 14px;
  overflow-y:auto;
  box-shadow: inset 0 0 30px rgba(255,255,255,0.04);
}

.client-root .chat-input{
  display:flex;
  gap: 12px;
  align-items:center;
}

.client-root .chat-input input{
  flex:1;
  padding: 14px 14px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(0,0,0,0.35);
  color: rgba(255,255,255,0.9);
  outline:none;
}

.client-root .chat-input input::placeholder{
  color: rgba(255,255,255,0.55);
}

.client-root .chat-input .btn{
  width: 120px;
}

/* subtle hum */
@keyframes hum {
  0% { filter: drop-shadow(0 0 0 rgba(0,255,255,0)); }
  50% { filter: drop-shadow(0 0 10px rgba(0,255,255,0.20)); }
  100% { filter: drop-shadow(0 0 0 rgba(0,255,255,0)); }
}



/* ================================
   COLOUR TUNING OVERRIDES (SAFE)
   This block only adjusts colours.
   No layout / spacing / font / sizing.
   ================================ */

.client-root {
  --zy-bg-1: #12001f;
  --zy-bg-2: #2b0b52;
  --zy-panel: rgba(22, 10, 40, 0.58);
  --zy-border: rgba(0,255,255, 0.22);
  --zy-text: #f4ecff;
  --zy-muted: #cdbbff;

  --zy-accent: #00ffff;
  --zy-accent-2: #d46bff;

  --zy-dark-btn: rgba(26, 12, 48, 0.92);
  --zy-dark-btn-border: rgba(150, 90, 255, 0.22);
}

/* Background tint only */
body {
  color: var(--zy-text);
}

/* Frosted panel tint only (won’t move anything) */
.client-root .panel,
.client-root .card,
.client-root .glass,
.client-root .panelGlass {
  background: var(--zy-panel);
  border-color: var(--zy-border);
}

/* Button colours only */
button,
.client-root .btn {
  border-color: var(--zy-dark-btn-border);
}

.client-root .btnPrimary,
.client-root .primary,
.client-root .button-primary {
  color: #fff;
}

.client-root .btnSecondary,
.client-root .secondary,
.client-root .button-secondary {
  background: var(--zy-dark-btn);
  color: var(--zy-text);
  border-color: var(--zy-dark-btn-border);
}

/* Text accents only */
h1, h2, h3, .title, .panelTitle {
  color: var(--zy-text);
}
.client-root .small, .muted, .subtle {
  color: var(--zy-muted);
}


.client-root .zy-panel-text {
  text-shadow: 0 0 6px rgba(160,120,255,0.6), 0 0 14px rgba(120,200,255,0.4);
  -webkit-font-smoothing: antialiased;
}

.client-root .zy-panel-text .metric,
.client-root .zy-panel-text .value,
.client-root .zy-panel-text strong {
  text-shadow: 0 0 8px rgba(160,120,255,0.75), 0 0 18px rgba(120,200,255,0.5);
}

.client-root .zy-panel-text {
  color: #f5f3ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 16px rgba(140,200,255,0.75),
    0 0 32px rgba(120,160,255,0.45);
}

.client-root .zy-panel-text .metric,
.client-root .zy-panel-text .value,
.client-root .zy-panel-text strong {
  text-shadow:
    0 0 8px rgba(200,140,255,1),
    0 0 22px rgba(160,220,255,0.9),
    0 0 40px rgba(120,160,255,0.6);
}

.client-root .zy-glowText {
  display: inline-block;
  filter:
    drop-shadow(0 0 4px rgba(200,140,255,0.85))
    drop-shadow(0 0 12px rgba(160,220,255,0.7));
}

.client-root .panel-analytics p,
.client-root .panel-status p,
.client-root .panel-notifications p,
.client-root .panel-notifications .inner-glass {
  font-size: 1.05rem;
  line-height: 1.65;
}

.client-root .panel-analytics .zy-glowText,
.client-root .panel-status .zy-glowText,
.client-root .panel-notifications .zy-glowText {
  font-size: 1.08rem;
}

/* disabled by Zypher UI */
.client-root .panel p span {
  font-size: 1.12rem;
  line-height: 1.7;
}

/* disabled by Zypher UI */
.client-root .panel p span.zy-glowText {
  font-size: 1.15rem;
}

.client-root .zy-glowText {
  text-shadow: none;
}

.client-root .panel-notifications .zy-glowText {
  filter: drop-shadow(0 0 6px rgba(160,220,255,0.65));
}

.client-root .status.online {
  background: rgba(80, 220, 160, 0.2);
  border-color: rgba(80, 220, 160, 0.5);
  color: #baffdc;
}

/* ===== MODE TOGGLE (FINAL, FLAT) ===== */
.client-root .btn.toggle {
  background: rgba(0,255,255,0.10);
  box-shadow: 0 0 24px rgba(0,180,255,0.22);
}

.client-root .btn.toggle.mode-test {
  background: linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72));
  box-shadow: 0 0 18px rgba(184,76,255,0.65);
}

.client-root .btn.toggle.mode-campaign {
  background: linear-gradient(180deg, rgba(0,255,255,0.14), rgba(0,0,0,0.72));
  box-shadow: 0 0 18px rgba(0,255,156,0.6);
}


/* ===== RADAR LANES (SINGLE SOURCE OF TRUTH) ===== */

.client-root .radar-lanes{
  position: absolute;
  inset: 0 0 78px 0; /* top, right, bottom (dock), left */
  display: flex;
  flex-direction: column;
  justify-content: space-evenly;
    overflow-y: hidden;
  overflow-x: visible;
  pointer-events: auto;
}

.client-root .radar-lane {
  flex: 1;
  display: flex;
  align-items: center;
  overflow-x: visible;
    overflow-y: hidden;
  white-space: nowrap;
}

/* ===== LEGACY RADAR POSITIONING (DISABLED) ===== */



.client-root .radar-track{ display:flex; width:max-content; }

.client-root .radar-track > * {
  flex-shrink: 0;
}



@keyframes radarScroll {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}

.client-root .controls > .btn:not(.radar-niche),
.client-root .chat-input > .btn:not(.radar-niche),
.client-root .panel .controls .btn:not(.radar-niche),
.client-root .panel .chat-input .btn:not(.radar-niche),
.client-root .panel .btn.toggle {
  width: 100%;
}


/* ===== RADAR BUTTON WIDTH FIX ===== */



/* ===== RADAR NICHE (FINAL SOURCE OF TRUTH) ===== */
.client-root .btn.radar-niche {
  pointer-events: auto;
  font-family: 'Orbitron', ui-sans-serif, system-ui;
  text-transform: capitalize;
  letter-spacing: 0.04em;
}

.client-root .panel-small { display: flex; flex-direction: column; min-height: 0; }


.client-root .panel-small:nth-of-type(1) { grid-column: 1; grid-row: 3; }
.client-root .panel-small:nth-of-type(2) { grid-column: 3; grid-row: 3; }


.client-root { overscroll-behavior: none; background: var(--bg0); }


.client-root .notif-item{
  opacity: 1;
  transition: opacity 1.2s ease, transform 0.6s ease;
  transform: translateY(0);
}

.client-root .notif-item.fade-out{
  opacity: 0;
  transform: translateY(-8px);
}





/* ===== NOTIFICATION FADE (STATE DRIVEN) ===== */

.client-root .notification-item {
  transition: opacity 0.6s ease, transform 0.6s ease;
  opacity: 1;
  transform: translateY(0);
}

.client-root .notification-item.dying {
  opacity: 0;
  transform: translateY(10px);
}


.client-root .panel-small .inner-glass {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  overflow-x: hidden;
}

/* ===============================
   NEURAL CORE — TOPOLOGY VIEW
   =============================== */

.client-root .neural-core{
  display:flex;
  flex-direction:column;
  gap:10px;
}

.client-root .neural-status{
  display:flex;
  justify-content:space-between;
  font-family:'Orbitron';
  font-size:11px;
  letter-spacing:0.12em;
}

.client-root .neural-status .online{
  color:#28d7ff;
  text-shadow:0 0 12px rgba(0,255,255,0.9);
}
.client-root .neural-status .offline{
  color:#00ffff;
  opacity:0.6;
}

.client-root .neural-topology{
  position: relative;

  position:relative;
  flex:1;
  margin:8px 0;
  display:flex;
  align-items:center;
  justify-content:center;
}

.client-root .node{
  position:absolute;
  width:10px;
  height:10px;
  border-radius:50%;
  background:#00ffff;
  box-shadow:0 0 14px rgba(0,255,255,0.9);
}

.client-root .node.core{
  position:absolute;
  top:50%;
  left:50%;
  width:10px;
  height:10px;
  border-radius:50%;
  background:#28d7ff;
  box-shadow:0 0 18px rgba(0,255,255,0.9);
  transform:translate(-50%,-50%);
}

/* orbit positions */
.client-root .n1{top:10%; left:50%; transform:translateX(-50%);}
.client-root .n2{top:30%; left:80%;}
.client-root .n3{top:70%; left:80%;}
.client-root .n4{top:90%; left:50%; transform:translateX(-50%);}
.client-root .n5{top:70%; left:20%;}
.client-root .n6{top:30%; left:20%;}

.client-root .link{
  position:absolute;
  width:2px;
  background:linear-gradient(#00ffff,#28d7ff);
  opacity:0.6;
  transform-origin:top;
  animation:pulse 2s infinite alternate;
}

.client-root .l1{ top:50%; left:50%; height:40%; transform:rotate(270deg);}
.client-root .l2{ top:50%; left:50%; height:35%; transform:rotate(330deg);}
.client-root .l3{ top:50%; left:50%; height:35%; transform:rotate(30deg);}
.client-root .l4{ top:50%; left:50%; height:40%; transform:rotate(90deg);}
.client-root .l5{ top:50%; left:50%; height:35%; transform:rotate(150deg);}
.client-root .l6{ top:50%; left:50%; height:35%; transform:rotate(210deg);}

@keyframes pulse{
  from{opacity:0.4;}
  to{opacity:1;}
}

.client-root .neural-console{
  font-family:monospace;
  font-size:11px;
  opacity:0.75;
  line-height:1.5;
}



/* ===== ZYPHER NEURAL TOPOLOGY (GEOMETRIC) ===== */

.client-root .neural-topology{
  position:relative;
  width:160px;
  height:160px;
  margin: 10px auto 0 auto;
}

.client-root .node{
  position:absolute;
  width:10px;
  height:10px;
  border-radius:50%;
  background:#00ffff;
  box-shadow:0 0 10px rgba(0,255,255,0.9);
}

.client-root .node.core{
  width:14px;
  height:14px;
  background:#28d7ff;
  box-shadow:0 0 18px rgba(0,255,255,1);
  top:50%;
  left:50%;
  transform:translate(-50%,-50%);
}

/* Hex ring — perfect 60° spacing, radius 55px */
.client-root .node.n1{ top:50%; left:50%; transform:translate(-50%,-50%) translate(0,-55px); }
.client-root .node.n2{ top:50%; left:50%; transform:translate(-50%,-50%) translate(47px,-28px); }
.client-root .node.n3{ top:50%; left:50%; transform:translate(-50%,-50%) translate(47px,28px); }
.client-root .node.n4{ top:50%; left:50%; transform:translate(-50%,-50%) translate(0,55px); }
.client-root .node.n5{ top:50%; left:50%; transform:translate(-50%,-50%) translate(-47px,28px); }
.client-root .node.n6{ top:50%; left:50%; transform:translate(-50%,-50%) translate(-47px,-28px); }

/* Neural links */
.client-root .link{
  position:absolute;
  width:2px;
  background:linear-gradient(#28d7ff,#00ffff);
  opacity:0.6;
  transform-origin:top;
}

/* 6 spokes */
.client-root .link.l1{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(0deg); }
.client-root .link.l2{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(60deg); }
.client-root .link.l3{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(120deg); }
.client-root .link.l4{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(180deg); }
.client-root .link.l5{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(240deg); }
.client-root .link.l6{ top:50%; left:50%; height:55px; transform:translateX(-50%) rotate(300deg); }



.client-root .radar-lane {
  overflow: visible;
}

.client-root .radar-track {
  display: flex;
  width: max-content;
}

.client-root .radar-track > * {
  margin-right: 14px;
}

.client-root .radar-lane:not(.radar-static) .radar-track {
  animation: radarMarquee 30s linear infinite;
}

.client-root .radar-static .radar-track {
  justify-content: center;
}

@keyframes radarMarquee {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}


/* ----- STATIC DEFAULT ROW ----- */
.client-root .radar-static{
  display:flex;
  justify-content:center;
}

.client-root .radar-static .radar-track{
  display:flex;
  justify-content:center;
  width:100%;
  transform:none !important;
  animation:none !important;
}

.client-root .radar-static .radar-niche{
  margin: 0 auto;
}


/* ----- CYBER SCROLL EFFECT ----- */
.client-root .radar-track{
  will-change: transform;
  backface-visibility: hidden;
  transform-style: preserve-3d;
}

.client-root .radar-lane:not(.radar-static) .radar-track{
  filter: blur(0.3px);
}

/* subtle motion blur illusion */
.client-root .radar-lane:not(.radar-static) .radar-track > *{
  filter: drop-shadow(0 0 6px rgba(255,0,255,0.45));
}

/* smoother conveyor */
@keyframes radarScroll {
  0%   { transform: translate3d(0,0,0); }
  100% { transform: translate3d(-50%,0,0); }
}


/* ===== LEAD PIPELINE ===== */

.client-root .lead-pipeline{
  display:flex;
  flex-direction:column;
  gap:14px;
  justify-content:space-between;
}

.client-root .pipeline-row{
  display:flex;
  flex-direction:column;
  gap:6px;
  font-size:12px;
  color: rgba(255,255,255,0.85);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.client-root .pipeline-row span{
  font-family: 'Orbitron', system-ui;
}

.client-root .pipeline-row .bar{
  width:100%;
  height:10px;
  background: rgba(0,0,0,0.45);
  border-radius:999px;
  overflow:hidden;
  border:1px solid rgba(180,80,255,0.35);
  box-shadow: inset 0 0 12px rgba(0,0,0,0.6);
}

.client-root .pipeline-row .bar > div{
  height:100%;
  background: linear-gradient(90deg, #00ffff, #00ffff);
  box-shadow: 0 0 12px rgba(180,80,255,0.85), 0 0 24px rgba(0,255,255,0.45);
  border-radius:999px;
  animation: pipelineGlow 2.8s ease-in-out infinite;
}

@keyframes pipelineGlow {
  0% { filter: brightness(0.9); }
  50% { filter: brightness(1.25); }
  100% { filter: brightness(0.9); }
}




/* ===== STATUS FONT OVERRIDE ===== */
/* Use Lead Pipeline (Orbitron) font for system text */
.client-root .panel-system-status .zy-glowText,
.client-root .panel-notifications .zy-glowText {
  font-family: 'Orbitron', system-ui;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
\n
/* ===== STATUS FONT OVERRIDE v3 ===== */

/* System Status panel (Backend, Mode) */
.client-root .panel h2 + p .zy-glowText,
.client-root .panel h2 + p + p .zy-glowText {
  font-family: 'Orbitron', system-ui;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

/* Explicit: Backend: Connected line (has data attribute) */
.client-root .panel [data-system-status].zy-glowText {
  font-family: 'Orbitron', system-ui !important;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

/* Notifications — same font but smaller */
.client-root .panel-notifications .zy-glowText {
  font-family: 'Orbitron', system-ui;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-size: 12px;
}

/* ===== CAMPAIGN AUTOPILOT ===== */

.client-root .campaign-autopilot .inner-glass{
  display:flex;
  flex-direction:column;
  justify-content:space-between;
  height:100%;
}

.client-root .autopilot-metric{
  display:flex;
  justify-content:space-between;
  align-items:center;
  font-size:13px;
  letter-spacing:0.08em;
  text-transform:uppercase;
}

.client-root .autopilot-metric span{
  color:rgba(255,255,255,0.6);
}

.client-root .autopilot-metric strong{
  font-size:18px;
  color:#d28cff;
  text-shadow:0 0 10px rgba(180,80,255,0.8);
}

.client-root .autopilot-state{
  margin-top:10px;
  padding:8px 10px;
  border-radius:10px;
  background:rgba(160,60,255,0.15);
  border:1px solid rgba(180,80,255,0.35);
  text-align:center;
  font-size:12px;
  letter-spacing:0.1em;
  text-transform:uppercase;
  color:#e4b3ff;
  box-shadow:0 0 18px rgba(160,60,255,0.45);
}

.client-root .autopilot-advice{
  margin-top:12px;
  font-size:11px;
  line-height:1.5;
  color:rgba(255,255,255,0.75);
  text-shadow:0 0 8px rgba(180,80,255,0.25);
}



/* === PANEL TEXT SCALE FIX (System Status + Notifications) === */
.client-root .panel-system-status .zy-glowText,
.client-root .panel-notifications .zy-glowText,
.client-root .panel-notifications p {
  font-size: 0.85em;
  line-height: 1.35;
  letter-spacing: 0.04em;
}


/* === PANEL CONTENT SCALE (System Status + Notifications) === */
.client-root .panel-system-status .zy-glowText,
.client-root .panel-notifications .zy-glowText {
  font-size: 0.82em;        /* slightly smaller than panel title */
  line-height: 1.25;
  letter-spacing: 0.04em;
}

/* === SYSTEM STATUS & NOTIFICATIONS TEXT SCALE FIX === */
.client-root .panel-system-status p,
.client-root .panel-system-status span,
.client-root .panel-notifications p,
.client-root .panel-notifications span {
  font-size: 12.5px;
  letter-spacing: 0.08em;
  line-height: 1.5;
  opacity: 0.9;
}

/* Keep panel titles bigger */
.client-root .panel-system-status h2,
.client-root .panel-notifications h2 {
  font-size: 15px;
  letter-spacing: 0.12em;
}

\n
/* ===== HARD OVERRIDE: System Status + Notifications text size ===== */
.client-root .panel-system-status p span,
.client-root .panel-system-status .zy-glowText,
.client-root .panel-notifications p span,
.client-root .panel-notifications .zy-glowText {
  font-size: 0.78rem !important;
  line-height: 1.4;
  letter-spacing: 0.09em;
}

/* Keep headers bigger */
.client-root .panel-system-status h2,
.client-root .panel-notifications h2 {
  font-size: 1.05rem !important;
}
\n
/* Restore glow for Notifications text */
.client-root .panel-notifications .zy-glowText {
  filter:
    drop-shadow(0 0 6px rgba(0,255,255,0.9))
    drop-shadow(0 0 14px rgba(140,80,255,0.6));
}
\n
/* Force System Status body text smaller than title */
.client-root .panel-system-status p,
.client-root .panel-system-status span {
  font-size: 0.78rem !important;
  letter-spacing: 0.08em;
}


/* ===== COMMAND CENTRE HUD FIX ===== */

/* System Status text */
.client-root .panel-system-status p,
.client-root .panel-system-status span {
  font-size: 0.78rem !important;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(220,200,255,0.95);
  text-shadow:
    0 0 8px rgba(150,80,255,0.45),
    0 0 20px rgba(120,60,255,0.25);
}

/* Notifications text */
.client-root .panel-notifications p,
.client-root .panel-notifications span {
  font-size: 0.78rem !important;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(220,200,255,0.95);
  text-shadow:
    0 0 8px rgba(150,80,255,0.45),
    0 0 20px rgba(120,60,255,0.25);
}

/* Titles stay larger */
.client-root .panel-system-status h2,
.client-root .panel-notifications h2 {
  font-size: 1rem;
  letter-spacing: 0.14em;
}


/* ===== HARD SYNC: SYSTEM STATUS TEXT SIZE = NOTIFICATIONS TEXT SIZE ===== */

/* This is the exact text for:
   BACKEND: CONNECTED
   MODE: TEST
*/
.client-root .panel-system-status [data-system-status].zy-glowText,
.client-root .panel-system-status p .zy-glowText {
  font-size: 12px !important;        /* match Notifications */
  line-height: 1.4 !important;
  letter-spacing: 0.08em !important;
  text-transform: uppercase;
}

/* Restore glow so it matches SYSTEM READY */
.client-root .panel-system-status [data-system-status].zy-glowText {
  filter:
    drop-shadow(0 0 4px rgba(200,140,255,0.9))
    drop-shadow(0 0 12px rgba(160,220,255,0.65)) !important;
}


/* ===== FINAL OVERRIDE: SYSTEM STATUS LINES (BACKEND/MODE) =====
   This targets the exact span attribute, no dependency on wrapper class.
*/
span[data-system-status].zy-glowText{
  font-size: 12px !important;
  line-height: 1.4 !important;
  letter-spacing: 0.08em !important;
  filter:
    drop-shadow(0 0 4px rgba(200,140,255,0.85))
    drop-shadow(0 0 12px rgba(160,220,255,0.65)) !important;
}


/* ===== FINAL: System Status lines match Notifications size + glow ===== */
.client-root .panel-system-status p,
.client-root .panel-system-status p span,
.client-root .panel-system-status .zy-glowText {
  font-size: 0.78rem !important;   /* matches your notifications look */
  line-height: 1.4 !important;
  letter-spacing: 0.08em !important;
}

.client-root .panel-system-status .zy-glowText{
  font-size: 0.78rem !important;
  line-height: 1.4 !important;
  letter-spacing: 0.08em !important;
}

/* ===== ZYPHER_NEON_STATUS ===== */
.client-root .panel-system-status .zy-glowText,
.client-root .panel-notifications .zy-glowText{
  color: #cfa8ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 14px rgba(0,255,255,0.65),
    0 0 28px rgba(120,200,255,0.45),
    0 0 48px rgba(80,180,255,0.25);
  filter:
    drop-shadow(0 0 6px rgba(0,255,255,0.85))
    drop-shadow(0 0 18px rgba(120,200,255,0.6));
}


/* ===== ZYPHER_FLOW_CONSOLE ===== */

.client-root .callflow-line{
  font-family: 'Orbitron', system-ui;
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 6px;
}

.client-root .panel-small .inner-glass{
  overflow-y:auto;
}


/* Lead Pipeline text neon */
.client-root .pipeline-row span{
  color:#f0e6ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 14px rgba(120,200,255,0.5);
}

/* ===== CALL FLOW NEON ===== */
.client-root .panel-small h2 + .inner-glass .zy-glowText{
  font-family: 'Orbitron', system-ui;
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #cfa8ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 14px rgba(120,200,255,0.6),
    0 0 28px rgba(120,200,255,0.35);
}


/* ===== Call Flow Empty State ===== */
.client-root .callflow-empty{
  display:flex;
  align-items:center;
  justify-content:center;
  height:100%;
  width:100%;
}



/* ===== CALL FLOW LAYOUT FIX (LEFT-ALIGNED) ===== */
.client-root .panel.panel-small .inner-glass{
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: flex-start;
  text-align: left;
  padding: 14px 14px;
  gap: 6px;
}

.client-root .callflow-line{
  width: 100%;
  display: block;
  text-align: left;
  margin: 0;
}

.client-root .panel.panel-small .inner-glass .zy-glowText{
  display: inline-block;
  text-align: left;
}


/* ===== CALLFLOW_CONSOLE_ALIGN ===== */
.client-root .callflow-console{
  overflow-y: auto;

  display:block !important;
  text-align:left !important;
  padding: 12px 14px !important;
}

.client-root .callflow-console .zy-glowText{
  display:block;
  text-align:left;
}

.client-root .callflow-console .callflow-line{
  margin: 0 0 6px 0;
}


/* ===== CALL FLOW NEON (match System Ready exactly) ===== */
.client-root .callflow-console .zy-glowText{
  color: #cfa8ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 14px rgba(0,255,255,0.65),
    0 0 28px rgba(120,200,255,0.45),
    0 0 48px rgba(80,180,255,0.25);
  filter:
    drop-shadow(0 0 6px rgba(0,255,255,0.85))
    drop-shadow(0 0 18px rgba(120,200,255,0.6));
}


/* ===== CALL FLOW ENTRY ANIMATION ===== */
.client-root .callflow-line{
  animation: callflowIn 420ms ease-out both;
}

@keyframes callflowIn{
  from{
    opacity: 0;
    transform: translateY(6px);
    filter: blur(1px);
  }
  to{
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
}


/* ===== CALL FLOW WAITING STATE ===== */
.client-root .callflow-console .waiting{
  animation: waitingPulse 2.4s ease-in-out infinite;
  opacity: 0.85;
}

@keyframes waitingPulse{
  0%   { opacity: 0.45; }
  50%  { opacity: 1; }
  100% { opacity: 0.45; }
}


.client-root .lead-pipeline .pipeline-row{
  width:100%;
  max-width:100%;
}

.client-root .lead-pipeline .pipeline-row span,
.client-root .lead-pipeline .pipeline-bar{
  width:100%;
}


.client-root .pipeline-metric{
  float: right;
  font-size: 11px;
  color: #cfa8ff;
  text-shadow:
    0 0 6px rgba(0,255,255,0.9),
    0 0 14px rgba(120,200,255,0.6);
}


/* ===== INJECTOR PANEL ===== */

/* ===== INJECTOR V1 FORMS ===== */

.client-root .injector-html, body{
  height: auto;
  min-height: 100%;
  overflow-y: auto;
  overscroll-behavior: auto;

body{
  overflow-y: auto;
  max-height: calc(100vh - 48px - 86px); /* panel height minus padding/title */
  padding-right: 6px; /* space for scrollbar */
}

.client-root .injector-section{
  background: var(--glass2);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 18px;
  padding: 14px;
  margin-bottom: 12px;
  box-shadow: inset 0 0 30px rgba(255,255,255,0.03);
}

.client-root .injector-section h3{
  margin: 0 0 10px 0;
  font-size: 0.95rem;
  letter-spacing: 0.10em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.88);
}

.client-root .injector-label{
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.65);
  margin-bottom: 6px;
}

.client-root .injector-input,
.client-root .injector-select,
.client-root .injector-textarea{
  width: 100%;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(15, 6, 28, 0.55);
  color: rgba(255,255,255,0.92);
  padding: 10px 12px;
  outline: none;
  margin-bottom: 10px;
}

.client-root .injector-textarea{
  resize: vertical;
  line-height: 1.35;
}

.client-root .injector-input::placeholder,
.client-root .injector-textarea::placeholder{
  color: rgba(255,255,255,0.45);
}

.client-root .injector-input:focus,
.client-root .injector-select:focus,
.client-root .injector-textarea:focus{
  border-color: rgba(0,255,255,0.45);
  box-shadow: 0 0 0 2px rgba(0,255,255,0.18);
}

.client-root .injector-hint{
  font-size: 11px;
  color: rgba(200,170,255,0.55);
  line-height: 1.25;
  margin: 4px 0 8px 4px;
  padding: 0;
  background: none;
  border: none;
  letter-spacing: 0.02em;
  text-shadow: 0 0 8px rgba(160,80,255,0.25);
}

.client-root .injector-preview{
  margin-top: 12px;
  font-size: 10px;
  opacity: 0.65;
  max-height: 160px;
  overflow: auto;
  background: rgba(0,0,0,0.25);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 14px;
  padding: 10px;
}


.client-root .injector-tab{
  position: fixed;
  right: 26px;
  top: 50%;
  transform: translateY(-50%);
  width: 56px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border-radius: 999px;
  cursor: pointer;
  z-index: 9999;
  animation: injectorDrift 3.8s ease-in-out infinite;
}

.client-root .injector-tab.open{
  background: transparent !important;  /* still NO BOX */
}

/* ===== INJECTOR DOCK (HARD LOCKED) ===== */


/* ===== ZYPHER FONT SYNC (Injector) ===== */
.client-root .injector-panel,
.client-root .injector-panel * {
  font-family: 'Orbitron', ui-sans-serif, system-ui !important;
  letter-spacing: 0.04em;
}
/* ===================================== */

.client-root .injector-panel{
  position: fixed;
  top: 24px;
  right: -380px;              /* fully off screen */
  width: 360px;
  height: calc(100vh - 48px);
  background: var(--glass);
  border-radius: 28px;
  backdrop-filter: blur(20px);
  box-shadow:
    -12px 0 40px rgba(0,0,0,0.6),
    0 0 60px rgba(0,255,255,0.25),
    0 0 40px rgba(0,255,255,0.15);
  padding: 22px;
  transition: right 420ms cubic-bezier(.2,1,.3,1);
  z-index: 20000;
  display: flex;
  flex-direction: column;
  pointer-events: auto;
}

.client-root .injector-panel.open{
  right: 94px;  /* arrow width + gap */
}









.client-root .injector-panel::before{
  z-index: 0;
  content:"";
  position:absolute;
  inset:-2px;
  background:
    radial-gradient(420px 220px at 30% 20%, rgba(0,255,255,0.16), transparent 60%),
    radial-gradient(420px 220px at 80% 10%, rgba(0,255,255,0.10), transparent 60%),
    radial-gradient(600px 260px at 50% 120%, rgba(0,180,255,0.10), transparent 60%);
  pointer-events:none;
  opacity:0.9;
}

.client-root .injector-panel > *{ position: relative; z-index: 1; }

.client-root .injector-panel h2{
  margin: 0 0 14px 0;
  font-family:'Orbitron', ui-sans-serif, system-ui;
  font-weight:700;
  letter-spacing: 0.10em;
  text-transform: uppercase;
  font-size: 16px;
  color: rgba(255,255,255,0.92);
  text-shadow: 0 0 18px rgba(0,229,255,0.28);
}

.client-root .injector-html, body{
  height: auto;
  min-height: 100%;
  overflow-y: auto;
  overscroll-behavior: auto;

body{
  flex:1;
  display:flex;
  flex-direction:column;
  gap:16px;

  background: var(--glass2);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 16px;
  padding: 16px;
  box-shadow: inset 0 0 30px rgba(255,255,255,0.04);

  max-height: calc(100vh - 140px);
  overflow-y: auto;
  max-height: calc(100vh - 120px);
  overflow-y: auto;
}


.client-root .injector-row{
  display:flex;
  justify-content: space-between;
  align-items:center;
  font-size: 13px;
  color: var(--muted);
}
/* ===== INJECTOR MINI PANELS (Command Centre style) ===== */

.client-root .injector-section{
  background: var(--glass2);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 16px;
  padding: 14px;
  box-shadow: inset 0 0 24px rgba(255,255,255,0.04);
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.client-root .injector-section h3{
  margin: 0 0 6px 0;
  font-family: 'Orbitron', ui-sans-serif, system-ui;
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.85);
}

.client-root .injector-field{
  display:flex;
  justify-content: space-between;
  align-items: center;
  font-size: 12px;
  color: var(--muted);
}

.client-root .injector-field strong{
  color: white;
  font-weight: 600;
}

.client-root .injector-status{
  display:flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
}

.client-root .injector-status .status-dot{
  width: 10px;
  height: 10px;
  border-radius: 50%;
  box-shadow: 0 0 8px currentColor;
}

.client-root .injector-status.on{ color: #28d7ff; }
.client-root .injector-status.off{ color: rgba(255,255,255,0.25); }



.client-root .injector-row strong{
  color: white;
}

.client-root .status-dot{
  width: 10px;
  height: 10px;
  border-radius: 50%;
  box-shadow: 0 0 8px currentColor;
}

.client-root .status-dot.off{
  color: rgba(255,255,255,0.25);
}


@keyframes injectorFloat {
  0% { transform: translateY(-50%) translateX(0); }
  50% { transform: translateY(-54%) translateX(-4px); }
  100% { transform: translateY(-50%) translateX(0); }
}

@keyframes injectorGlow {
  0% {
    box-shadow:
      0 0 40px rgba(0,255,255,0.65),
      0 0 80px rgba(0,255,255,0.25);
  }
  50% {
    box-shadow:
      0 0 60px rgba(0,255,255,0.95),
      0 0 120px rgba(0,255,255,0.45);
  }
  100% {
    box-shadow:
      0 0 40px rgba(0,255,255,0.65),
      0 0 80px rgba(0,255,255,0.25);
  }
}




.client-root .injector-tab::after{
  content: "";
  position: absolute;
  inset: -16px;
  border-radius: 999px;

  /* pure light (still “transparent”) */
  background:
    radial-gradient(circle at 50% 50%,
      rgba(0,255,255,0.18),
      rgba(0,255,255,0.10) 45%,
      rgba(0,255,255,0.00) 72%
    );

  filter: blur(10px);
  opacity: 0.95;

  pointer-events: none;

  animation: injectorHum 2.8s ease-in-out infinite;
}


.client-root .injector-tab:hover::before{
  transform: translateX(8px) scale(1.05);
  filter: brightness(1.4);
}


@keyframes injectorPulse {
  0% { opacity: 0.7; transform: scale(1) translateX(0); }
  50% { opacity: 1; transform: scale(1.08) translateX(6px); }
  100% { opacity: 0.7; transform: scale(1) translateX(0); }
}


@keyframes injectorDrift {
  0% { transform: translateY(-50%) translateX(0); }
  50% { transform: translateY(-54%) translateX(-8px); }
  100% { transform: translateY(-50%) translateX(0); }
}


.client-root .injector-tab::before{
  content: "›";
  font-family: 'Orbitron', system-ui;
  font-size: 64px;
  font-weight: 700;
  color: rgba(255,255,255,0.95);
  text-shadow:
    0 0 12px rgba(0,255,255,1),
    0 0 30px rgba(0,255,255,0.9),
    0 0 70px rgba(0,255,255,0.6);
  filter: drop-shadow(0 0 20px rgba(0,255,255,1));
  display:inline-block;
  transform: rotate(0deg);
  transition: transform 0.5s cubic-bezier(.2,1.6,.3,1), filter 0.4s, text-shadow 0.4s;
}

.client-root .injector-tab.open::before{
  transform: rotate(180deg);
  color: #28d7ff;
  text-shadow:
    0 0 16px rgba(0,255,255,1),
    0 0 40px rgba(0,255,255,1),
    0 0 90px rgba(0,255,255,0.7);
}

/* ===== INJECTOR LAYOUT TUNING ===== */

.client-root .injector-panel h2,
.client-root .injector-panel h3 {
  letter-spacing: 0.08em;
  font-weight: 500;
}

.client-root .injector-label {
  font-size: 11px;
  letter-spacing: 0.12em;
  opacity: 0.7;
  margin-bottom: 4px;
}

.client-root .injector-input,
.client-root .injector-select {
  padding: 10px 12px;
  font-size: 12px;
  border-radius: 12px;
  letter-spacing: 0.04em;
}

.client-root .injector-section {
  margin-bottom: 20px;
}

.client-root .infra-group {
  margin-bottom: 16px;
}

.client-root .infra-credentials {
  margin-top: 6px;
}

.client-root .injector-hint {
  font-size: 10px;
  padding: 4px 2px;
  background: transparent;
  border: none;
  opacity: 0.55;
  letter-spacing: 0.06em;
}

.client-root .infra-topology .infra-map {
  font-size: 10px;
  line-height: 1.45;
  letter-spacing: 0.06em;
  padding: 8px 10px;
  border-radius: 12px;
}

/* Make scroll area feel more HUD-like */
.client-root .injector-body {
  padding-right: 12px;
}

/* ================================= */



/* ================================
   ZYPHER CAMPAIGN GREEN — CLIENT UI
   Source: .btn.toggle.mode-campaign
   #00ff9c → #00c87a
================================ */

.client-root .panel{
  position:relative;
  border-radius:22px;
  padding:22px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.08),
      rgba(0,0,0,0.65)
    );

  border:1px solid rgba(0,255,255,0.55);
  backdrop-filter: blur(18px);

  box-shadow:
    0 0 28px rgba(0,255,255,0.35),
    inset 0 0 18px rgba(0,255,255,0.15);

  overflow:hidden;
  display:flex;
  flex-direction:column;
}


.client-root .inner-glass{
  flex:1;
  border-radius:16px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.06),
      rgba(0,0,0,0.7)
    );

  border:1px solid rgba(0,255,255,0.35);
  box-shadow:
    inset 0 0 20px rgba(0,255,255,0.12);

  padding:14px;
  color: rgba(200,255,255,0.9);
}


.client-root .panel h2,
.client-root .panel p,
.client-root .panel span,
.client-root .panel div {
  color: #baffdc !important;
  text-shadow:
    0 0 12px rgba(0,255,156,0.8),
    0 0 30px rgba(0,255,156,0.45) !important;
}


/* ================================
   COMMAND CENTRE GRID SCALE
================================ */

.client-root .app {
  max-width: 1600px !important;
  padding: 60px 80px !important;
}

.client-root .grid {
  grid-template-columns: repeat(3, minmax(420px, 1fr)) !important;
  grid-auto-rows: 360px !important;
  gap: 40px !important;
}

.client-root .panel{
  position:relative;
  border-radius:22px;
  padding:22px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.08),
      rgba(0,0,0,0.65)
    );

  border:1px solid rgba(0,255,255,0.55);
  backdrop-filter: blur(18px);

  box-shadow:
    0 0 28px rgba(0,255,255,0.35),
    inset 0 0 18px rgba(0,255,255,0.15);

  overflow:hidden;
  display:flex;
  flex-direction:column;
}


.client-root .inner-glass{
  flex:1;
  border-radius:16px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.06),
      rgba(0,0,0,0.7)
    );

  border:1px solid rgba(0,255,255,0.35);
  box-shadow:
    inset 0 0 20px rgba(0,255,255,0.12);

  padding:14px;
  color: rgba(200,255,255,0.9);
}



/* ===============================
   ZYPHER CLIENT – CAMPAIGN GREEN
   =============================== */


  --neonA:#00ffff;
  --neonB:#00ffff;
  --neonC:#00ffff;

  --glow: 0 0 36px rgba(0,255,255,0.35);
  --glow2: 0 0 28px rgba(0,255,255,0.22);
}


  background:
    radial-gradient(900px 420px at 58% 6%, rgba(0,255,156,0.18), transparent 70%),
    radial-gradient(700px 360px at 18% 18%, rgba(0,255,200,0.12), transparent 65%),
    radial-gradient(1200px 700px at 50% 120%, rgba(0,255,156,0.10), transparent 60%),
    linear-gradient(180deg, #00140e, #000b07 45%, #000402);
}

.client-root .panel{
  position:relative;
  border-radius:22px;
  padding:22px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.08),
      rgba(0,0,0,0.65)
    );

  border:1px solid rgba(0,255,255,0.55);
  backdrop-filter: blur(18px);

  box-shadow:
    0 0 28px rgba(0,255,255,0.35),
    inset 0 0 18px rgba(0,255,255,0.15);

  overflow:hidden;
  display:flex;
  flex-direction:column;
}


.client-root .inner-glass{
  flex:1;
  border-radius:16px;

  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.06),
      rgba(0,0,0,0.7)
    );

  border:1px solid rgba(0,255,255,0.35);
  box-shadow:
    inset 0 0 20px rgba(0,255,255,0.12);

  padding:14px;
  color: rgba(200,255,255,0.9);
}


.client-root .panel h2,
.client-root .panel p,
.client-root .panel span,
.client-root .panel div{
  color: #baffdc;
  text-shadow:
    0 0 10px rgba(0,255,156,0.55),
    0 0 24px rgba(0,255,156,0.35);
}

.client-root .btn.primary{
  background: linear-gradient(90deg, #00ff9c, #00c87a);
  box-shadow: 0 0 30px rgba(0,255,156,0.65), 0 0 18px rgba(0,255,156,0.35);
}

}



/* ============================
   SCI-FI CLIENT SCROLLBAR (SMOOTH)
   ============================ */

/* Chrome / Edge / Safari */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(0,255,255,0.75);
  border-radius: 999px;

  /* IMPORTANT: no inset shadows (causes jank) */
  box-shadow: 0 0 10px rgba(0,255,255,0.45);

  /* Force GPU compositing */
  will-change: transform;
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(0,255,255,0.95);
  box-shadow: 0 0 14px rgba(0,255,255,0.65);
}

/* Firefox */
* {
  scrollbar-width: thin;
  scrollbar-color: rgba(0,255,255,0.8) transparent;
}


/* =====================================================
   FINAL PANEL OVERRIDE — MUST WIN CASCADE
   Purpose: force auth-blue glass on dashboard panels
   ===================================================== */

.client-root .grid .panel {
  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.14),
      rgba(0,0,0,0.70)
    ) !important;

  border: 1px solid rgba(0,255,255,0.65) !important;

  box-shadow:
    0 0 42px rgba(0,255,255,0.55),
    inset 0 0 22px rgba(0,255,255,0.22) !important;
}


/* =====================================================
   CYAN THEME LOCK — NO PURPLE ALLOWED
   ===================================================== */

.client-root {
  --neonA: #00ffff !important;
  --neonB: #00ffff !important;
  --neonC: #00ffff !important;

  --zy-accent: #00ffff !important;
  --zy-border: rgba(0,255,255,0.65) !important;
  --zy-panel: rgba(0,40,50,0.65) !important;

  --text: #eaffff !important;
  --muted: rgba(200,255,255,0.75) !important;
}


/* ZY_DASH_BG_CYAN_LOCK */
.client-root {
  /* cyan-biased base so glass never picks up purple */
  background:
    radial-gradient(900px 420px at 58% 6%, rgba(0,255,255,0.18), transparent 70%),
    radial-gradient(700px 360px at 18% 18%, rgba(0,255,255,0.10), transparent 65%),
    linear-gradient(180deg, #020814, #020b18 45%, #00040a) !important;
}


/* ZY_CYAN_HARD_LOCK */
/* 100% PURPLE ELIMINATION – CLIENT DASHBOARD ONLY */

.client-root {
  --zy-cyan: rgb(0, 255, 255);
  --zy-cyan-soft: rgba(0,255,255,0.25);
  --zy-cyan-dim: rgba(0,255,255,0.14);
  --zy-dark-cyan: rgb(0, 8, 16);
}

/* Kill violet drop-shadows completely */
.client-root .panel,
.client-root .panel * {
  filter: none !important;
}

/* Force cyan-only panel glass */
.client-root .grid .panel {
  background:
    linear-gradient(
      180deg,
      rgba(0,255,255,0.22),
      rgb(0, 6, 12)
    ) !important;

  border: 1px solid rgba(0,255,255,0.65) !important;

  box-shadow:
    0 0 42px rgba(0,255,255,0.45),
    inset 0 0 22px rgba(0,255,255,0.22) !important;
}

/* Cyan-only glow layer */
.client-root .panel::before {
  background:
    radial-gradient(
      420px 220px at 50% 0%,
      rgba(0,255,255,0.22),
      transparent 68%
    ) !important;
}

/* Cyan-biased page base (prevents bleed-through) */
.client-root {
  background:
    radial-gradient(900px 420px at 58% 6%, rgba(0,255,255,0.22), transparent 70%),
    radial-gradient(700px 360px at 18% 18%, rgba(0,255,255,0.14), transparent 65%),
    linear-gradient(180deg, rgb(0,6,12), rgb(0,4,10) 45%, rgb(0,2,6)) !important;
}


/* ZY_EQUAL_PANEL_LAYOUT */
/* ================================
   EQUAL PANEL GRID – FULL PAGE
   ================================ */

.client-root {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.client-root .app {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.client-root .grid {
  flex: 1;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: repeat(2, minmax(0, 1fr));
  gap: 28px;
  align-content: stretch;
}

.client-root .panel {
  min-height: 0;
  height: 100%;
}


/* ZY_PANEL_IDENTITY */
/* ================================
   PANEL IDENTITY & FOCUS
   ================================ */

.client-root .panel::after {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
  box-shadow:
    inset 0 32px 48px rgba(0,255,255,0.08);
}


/* ZY_CALM_MOTION */
/* ================================
   CALM STATUS MOTION
   ================================ */

@keyframes zy-breathe {
  0% { box-shadow: 0 0 36px rgba(0,255,255,0.35); }
  50% { box-shadow: 0 0 52px rgba(0,255,255,0.55); }
  100% { box-shadow: 0 0 36px rgba(0,255,255,0.35); }
}

.client-root .panel {
  animation: zy-breathe 6s ease-in-out infinite;
}


/* ZY_FULL_HEIGHT_GRID_FIX */
/* ================================
   FULL HEIGHT GRID (NO SQUISH)
   ================================ */

/* Root must own the viewport */
.client-root {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

/* Header stays natural height */
.client-root .header {
  flex: 0 0 auto;
}

/* App fills remaining space */
.client-root .app {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

/* Grid fills remaining space AFTER header */
.client-root .grid {
  flex: 1;
  display: grid;

  grid-template-columns: repeat(3, 1fr);

  /* KEY FIX: tall rows, not fractional squish */
  grid-template-rows: repeat(2, minmax(320px, 1fr));

  gap: 28px;
  align-content: stretch;
  min-height: 0;
}

/* Panels stretch fully */
.client-root .panel {
  height: 100%;
  min-height: 0;
}


/* ZY_FORCE_FULL_HEIGHT */
/* ======================================
   FORCE DASHBOARD TO FILL VIEWPORT
   ====================================== */

html, body {
  height: 100%;
  margin: 0;
}

#root {
  height: 100%;
}

.client-root {
  height: 100%;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.client-root .header {
  flex: 0 0 auto;
}

.client-root .app {
  flex: 1 1 auto;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.client-root .grid {
  flex: 1 1 auto;
  min-height: 0;

  display: grid;
  grid-template-columns: repeat(3, 1fr);

  /* THIS is the real fix */
  grid-template-rows: repeat(2, 1fr);

  align-items: stretch;
  justify-items: stretch;
}

.client-root .panel {
  height: 100%;
}


/* ZY_FORCE_FULL_WIDTH */
/* ======================================
   FORCE PANELS TO STRETCH HORIZONTALLY
   ====================================== */

.client-root .app {
  width: 100%;
  max-width: none;
  padding-left: 2vw;
  padding-right: 2vw;
}

.client-root .grid {
  width: 100%;
  max-width: none;

  grid-template-columns: repeat(3, minmax(0, 1fr));
  column-gap: 2.2vw;
  row-gap: 2.4vh;
}

.client-root .panel {
  width: 100%;
}
